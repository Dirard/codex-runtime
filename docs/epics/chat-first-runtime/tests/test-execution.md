# QA Verification: Chat-First Runtime

Type: test execution
Artifact purpose / consumer: future execution and evidence register for the chat-first runtime epic.
Status: partial implementation checks started; Stage 03, Stage 05, Stage 06, Stage 07, Stage 08, Stage 09, and Stage 10 checks added; Stage 09 repair checks and targeted owner re-review passed; 2026-06-16 working-copy baseline QA rerun executed; 2026-06-16 QA repair follow-up, gap-closure reruns, and raw-public-contract caveat follow-up executed; targeted Stage 09 chat-runtime blocker coverage is clean; full QA coverage still partial
Owner: qa_engineer
Last repaired: 2026-06-16
Related docs: `test-strategy.md`, `test-cases.md`, `regression.md`, `../reviews/qa-readiness.md`
Trace IDs: TC-001..TC-036, QA-EVID-001..QA-EVID-036

## Source Of Truth
This artifact owns execution evidence after implementation exists. Stage 02 contract checks and targeted Stage 03/05/06/07/08/09/10 implementation checks have executed. A 2026-06-16 working-copy baseline QA rerun executed the baseline verification set, a later 2026-06-16 QA gap-closure rerun closed the prior direct-evidence gaps for `TC-005`, `TC-009`, `TC-027`, and `TC-031`, and a raw-public-contract caveat follow-up verified removal of the legacy public raw task event surface. The full `TC-001`..`TC-036` and `REG-001`..`REG-012` proof set is still not complete.

## Executed Scope
- Stage 02 contract compile/descriptor checks executed for the chat-first proto and generated Go surface.
- Stage 03 local readiness/security-boundary checks executed for chat runtime config, auth-gated chat service registration, disabled behavior, gRPC health status, task compatibility preservation, and the minimal app-server supervisor foundation.
- Stage 05 start boundary checks executed for `StartChatRun` validation, Codex thread/turn acceptance, same-process idempotent success retry, unknown-delivery duplicate suppression, and chat-specific gRPC errors.
- Stage 06 existing-chat checks executed for `GetChat`, `RunChatTurn`, status, history, stream/replay, cancellation, unknown lifecycle handling, signed history cursors, stream subscription closure, and canonical Codex thread id validation.
- Stage 07 pending/interrupt checks executed for app-server forwarded-request claiming, active-run pending correlation, pending response forwarding/idempotent retry, unavailable-after-run-loss behavior, and Codex interrupt forwarding without fabricated terminal completion.
- Stage 08 Go SDK/adoption checks executed for SDK `Run`, `GetChat`, `chat.Run`, status, history, event streaming, pending responses, interrupt, typed error mapping, SDK-side validation, generated safe IDs, stream close semantics, auth metadata handling, token redaction, and sanitized local API-handler example behavior.
- Stage 09 integration hardening checks executed for full package tests, package discovery, vet, diff hygiene, legacy naming/import absence, no SQLite/local DB implementation markers, no Desktop UI promise markers in Go code, and raw/unknown payload confinement to task compatibility plus chat contract guardrails.
- Stage 10 volatile correlation checks executed for process-local active runs, replay/cursors, pending, idempotency, diagnostics, and restart-loss behavior.
- Full `TC-001`..`TC-036` QA execution remains pending until the final implementation baseline is ready.

## Implementation References
- Implementation reference(s): `proto/codex_control/v1/codex_control.proto`, `gen/codex/control/v1/codex_control.pb.go`, `gen/codex/control/v1/codex_control_grpc.pb.go`, `gateway/internal/grpcapi/chat_contract_test.go`, `gateway/internal/config/types.go`, `gateway/internal/config/toml.go`, `gateway/internal/config/validate.go`, `gateway/internal/grpcapi/server.go`, `gateway/internal/grpcapi/health.go`, `gateway/internal/grpcapi/chat_handlers.go`, `gateway/internal/grpcapi/chat_handlers_test.go`, `gateway/internal/grpcapi/server_test.go`, `gateway/internal/grpcapi/validators.go`, `gateway/internal/grpcapi/validators_test.go`, `gateway/internal/appserver/supervisor.go`, `gateway/internal/appserver/supervisor_test.go`, `gateway/internal/chatruntime/doc.go`, `gateway/internal/chatruntime/service.go`, `gateway/internal/chatruntime/errors.go`, `gateway/internal/chatruntime/service_test.go`, `gateway/internal/chatruntime/stream.go`, `gateway/internal/chatstate/doc.go`, `gateway/internal/chatstate/types.go`, `gateway/internal/chatstate/store.go`, `gateway/internal/chatstate/store_test.go`, `gateway/internal/domain/commands.go`, `gateway/internal/domain/errors.go`, `gateway/internal/grpcapi/service_errors.go`
- Stage 07 implementation reference(s): `gateway/internal/appserver/connection.go`, `gateway/internal/chatruntime/pending_interrupt.go`, `gateway/internal/chatruntime/service.go`, `gateway/internal/chatruntime/stream.go`, `gateway/internal/chatruntime/service_test.go`, `gateway/internal/chatstate/types.go`, `gateway/internal/chatstate/store.go`, `gateway/internal/domain/commands.go`, `gateway/internal/domain/events.go`, `gateway/internal/grpcapi/chat_handlers.go`, `gateway/internal/grpcapi/chat_handlers_test.go`, `gateway/internal/grpcapi/service_interfaces.go`, `gateway/internal/grpcapi/validators.go`
- Stage 08 implementation reference(s): `client.go`, `chat.go`, `history.go`, `stream.go`, `pending.go`, `types.go`, `client_test.go`, `examples/api-handler/main.go`, `examples/api-handler/main_test.go`
- Stage 09 integration reference(s): all Go packages in `go list ./...`, `gateway/internal/chatstate/store.go`, `gateway/internal/chatstate/store_test.go`, `gateway/internal/chatruntime/service.go`, `gateway/internal/chatruntime/service_test.go`, `scripts/generate-proto.ps1`, `docs/epics/chat-first-runtime/plan/stage-09.md`, `docs/epics/chat-first-runtime/tests/test-execution.md`, `docs/epics/chat-first-runtime/README.md`
- Code owner: developer implementation slice under root supervision

## Planned Evidence Groups
| Evidence ID range | Planned scope | Required implementation reference or area when execution starts |
| --- | --- | --- |
| `QA-EVID-001`..`QA-EVID-008` | start/get/run identity, first-turn acceptance, and continuation behavior | chat-first SDK surface, `ChatRuntimeService`, Codex adapter |
| `QA-EVID-009`..`QA-EVID-018` | status, history depth, live stream, replay, and cancellation behavior | status normalization, history adapter, stream/replay buffer, cursor handling |
| `QA-EVID-019`..`QA-EVID-027` | pending, interrupt, idempotency, and restart-loss behavior | pending correlation, interrupt path, current-process idempotency, restart handling |
| `QA-EVID-028`..`QA-EVID-032` | all-RPC auth/authz/validation, redaction, and no-side-effect proofs | auth middleware, request validation, redaction/diagnostics surfaces |
| `QA-EVID-033`..`QA-EVID-036` | config, readiness, supervisor/backoff, coexistence, and paper guardrails | config loader, readiness/health, service registration, runbook/observability surfaces |

## Execution Traceability
Feature/high-risk execution must map every executed check to a concrete implementation area and `QA-EVID-*`.

| Test or check ref | Requirement and product-doc refs | Implementation reference or area verified | Result | QA evidence ID | Evidence link | Notes or waiver |
| --- | --- | --- | --- | --- | --- | --- |
| `TC-001`..`TC-036` | see `test-cases.md` | pending final implementation baseline | not run | `QA-EVID-001`..`QA-EVID-036` | pending | full execution starts after final implementation and docs sync baseline |
| Stage 02 contract compile/descriptor checks | `SRS-FR-001`, `SRS-FR-003`, `SRS-FR-014`, `SRS-FR-018`, `SRS-FR-019`, `REG-001`, `REG-002`, `REG-011` | chat-first proto/generated Go service split and descriptor guardrails | pass | `QA-EVID-034` partial | `GOPROXY=off go test ./...` | Verifies separate `ChatRuntimeService`, chat identity fields, and no raw payload escape hatch on `ChatEvent`. |
| Stage 03 local readiness/security-boundary checks | `SRS-FR-013`, `SRS-FR-014`, `SRS-FR-017`, `SRS-FR-019`, `SRS-NFR-002`, `SRS-NFR-004`, `SRS-NFR-007`, `SRS-NFR-008`, `REG-010`, `REG-011`, `REG-012` | config parser/validator, chat runtime disable path, chat gRPC registration, auth interceptors, health status, task compatibility, app-server supervisor foundation | pass | `QA-EVID-028`..`QA-EVID-036` partial | `GOPROXY=off go test ./...` | Verifies `[chat_runtime].enabled`, auth-gated chat RPCs, authorization metadata stripping before chat handler, typed disabled result, health auth, unknown health service without request-detail disclosure, NOT_SERVING chat health while disabled, task RPC preservation, lazy shared cold start, startup timeout as an upper bound even with a longer caller deadline, shorter caller-deadline precedence, repeated-failure backoff, typed retryable backoff status, health NOT_SERVING from supervisor backoff without an extra app-server start, connecting/closed status, and close cancellation. |
| Stage 05 start boundary checks | `SRS-FR-004`, `SRS-FR-008`, `SRS-FR-011`, `SRS-FR-013`, `SRS-FR-018`, `SRS-FR-019`, `SRS-NFR-003`, `SRS-NFR-005`, `SRS-NFR-009`, `REG-002`, `REG-003`, `REG-009`, `REG-010`, `REG-011` | `chatruntime.Service`, `ChatRuntimeService.StartChatRun` handler/validator, process-local idempotency result refs, command composition wiring | pass | `QA-EVID-001`..`QA-EVID-003`, `QA-EVID-024`, `QA-EVID-026`, `QA-EVID-028`..`QA-EVID-032`, `QA-EVID-034` partial | `GOPROXY=off go test ./...` | Verifies empty prompt rejection before Codex calls, supervisor failure release before side effect, `thread/start` then `turn/start` success with `chat_id == Thread.id` and `run_id == Turn.id`, active run correlation, successful same-key retry without duplicate Codex calls, same-key/different-`client_message_id` conflict without duplicate Codex calls, uncertain post-thread/turn failure returning typed unknown without duplicate calls, idempotency scope conflict before Codex calls, gRPC chat-specific error details, and no prompt leakage in sanitized failures. |
| Stage 06 existing-chat checks | `SRS-FR-003`, `SRS-FR-005`, `SRS-FR-006`, `SRS-FR-007`, `SRS-FR-008`, `SRS-FR-010`, `SRS-FR-011`, `SRS-FR-012`, `SRS-FR-014`, `SRS-FR-016`, `SRS-FR-018`, `SRS-FR-019`, `REG-002`, `REG-004`, `REG-005`, `REG-006`, `REG-007`, `REG-009`, `REG-010`, `REG-011` | `chatruntime.Service`, `chatstate.Store`, `ChatRuntimeService` handlers/validators, history cursor envelope, stream/replay store | pass | `QA-EVID-004`..`QA-EVID-018`, `QA-EVID-027`, `QA-EVID-028`..`QA-EVID-032` partial | `GOPROXY=off go test -count=1 ./gateway/internal/chatstate ./gateway/internal/chatruntime ./gateway/internal/domain ./gateway/internal/grpcapi`; `GOPROXY=off go test -count=1 ./...` | Verifies Codex-thread-id-shaped `chat_id` validation, `GetChat` and existing turn behavior, active-run conflict, unknown thread lifecycle returning retryable typed `chat_state_unavailable` without clearing local active run, HMAC-signed history cursors scoped to session/workspace/chat/depth/sort, tampered and cross-chat cursor rejection, stream subscribe closure after terminal run, stream cancellation without interrupt, and no raw JSONL/public payload escape hatch. |
| Stage 07 pending/interrupt checks | `SRS-FR-006`, `SRS-FR-009`, `SRS-FR-010`, `SRS-FR-011`, `SRS-FR-012`, `SRS-FR-013`, `SRS-FR-019`, `SRS-NFR-003`, `SRS-NFR-004`, `SRS-NFR-005`, `SRS-NFR-007`, `SRS-NFR-009`, `REG-008`, `REG-009`, `REG-010`, `REG-011`, `REG-012` | app-server forwarded request hook, chat pending correlation, pending response forwarding, interrupt forwarding, stream payload mapping, gRPC validators/handlers | pass after targeted repair and owner re-review | `QA-EVID-019`..`QA-EVID-027`, `QA-EVID-028`..`QA-EVID-032`, `QA-EVID-035` partial | `GOPROXY=off go test -count=1 .\gateway\internal\appserver .\gateway\internal\chatstate .\gateway\internal\chatruntime .\gateway\internal\grpcapi`; `GOPROXY=off go test -count=1 ./...`; `git diff --check` | Verifies chat pending claims use the single app-server request stream, active run/session/run correlation, stream pending event delivery, pending response forwarding with idempotent retry suppression, expired pending rejection before Codex write, expired wrong-shape pending rejection before type disclosure, idempotency scope including `pending_request_id`, process-local pending record cleanup, lazy cleanup of expired raw pending records on later pending trigger/status read, no pending-response content fingerprint retention in chat runtime, pending response after interrupt and during response-write interleaving preserving `interrupting`, pending creation refusing to overwrite `interrupting`, typed unavailable when run correlation is gone, Codex interrupt forwarding, and no fabricated terminal completion after interrupt. |
| Stage 10 volatile correlation checks | `SRS-FR-006`, `SRS-FR-008`, `SRS-FR-009`, `SRS-FR-011`, `SRS-FR-018`, `SRS-FR-019`, `SRS-NFR-003`, `SRS-NFR-007`, `SRS-NFR-009`, `REG-002`, `REG-007`, `REG-008`, `REG-009`, `REG-011`, `REG-012` | process-local chatstate store, safe domain reasons, gRPC aborted mapping | pass | `QA-EVID-015`..`QA-EVID-027`, `QA-EVID-032`, `QA-EVID-035` partial | `GOPROXY=off go test ./...` | Verifies active run conflict until terminal, active-run cap and terminal cleanup, replay epoch/scope/bounds, replay TTL on read after idle time, restart loss for active/replay/pending/idempotency/diagnostics, pending cap/expiry/duplicate resolution/cleanup, idempotency cap/cleanup and safe-scope conflict without content fingerprints, capped diagnostics, and no prompt/response/raw-payload/hash/digest fields in correlation records. |

## Stage 08 Supplemental Execution Trace
Stage 08 SDK/adoption checks map to `SRS-FR-001`, `SRS-FR-003`, `SRS-FR-004`, `SRS-FR-005`, `SRS-FR-006`, `SRS-FR-007`, `SRS-FR-008`, `SRS-FR-009`, `SRS-FR-010`, `SRS-FR-011`, `SRS-FR-013`, `SRS-FR-014`, `SRS-FR-016`, `SRS-FR-018`, `SRS-FR-019`, `SRS-NFR-001`, `SRS-NFR-002`, `SRS-NFR-006`, `SRS-NFR-009`, and related regression cases `REG-002`, `REG-004`, `REG-007`, `REG-008`, `REG-010`, `REG-011`, `REG-012`.

Verified implementation areas: root Go SDK package, gRPC client wrapping, SDK typed errors, stream wrapper, pending/interrupt helpers, and sanitized API handler example. Result: pass after targeted repairs and owner re-review. Evidence commands: `GOPROXY=off go test -count=1 .`; `GOPROXY=off go test -count=1 .\examples\api-handler`; `GOPROXY=off go test -count=1 ./...`; `GOPROXY=off go list ./...`; `GOPROXY=off go vet ./...`; `git diff --check`.

Verified behaviors include SDK-side empty prompt rejection before RPC, `chat_id == Codex Thread.id` handle behavior, accepted start/turn response invariants, generated safe client ids/idempotency keys for side-effecting helpers, typed status/history/error mapping, `ResourceExhausted` normalization, stream cursor options and `Close` cancellation without interrupt, caller metadata stripping with exactly one bearer header, token redaction from SDK string forms, local loopback-only insecure example guard, generic HTTP error output, and no Desktop UI/current-thread promise.

## Stage 09 Supplemental Execution Trace
Stage 09 integration hardening checks map to all active SRS FR/NFR guardrails and regression cases `REG-001`..`REG-012`.

Verified implementation areas: root SDK package, API-handler example, gateway command, gateway internals, task compatibility packages, generated package, proto generation script, and documentation evidence registers. Result: local checks pass after engineering/QA repair, with targeted owner re-review pass recorded. Evidence commands: `GOPROXY=off go test -count=1 ./...`; `GOPROXY=off go list ./...`; `GOPROXY=off go vet ./...`; `powershell -ExecutionPolicy Bypass -File .\scripts\generate-proto.ps1`; `gofmt -w .\gen`; `git diff --check`; file-tools grep checks over Go/source-code paths for legacy naming/imports, SQLite/local DB markers, Desktop UI promise markers, and raw/unknown payload confinement.

Verified guardrails include service/package compile coverage, task compatibility coexistence, `StartChatRun` active-run capacity reservation before Codex side effects, proto generation sync, no old `codex-control-gateway` implementation references in Go/source-code paths, no old root internal imports in Go files, no SQLite/local DB implementation markers in Go code, no Desktop UI/current-thread promise markers in Go code, and raw/unknown payload markers limited to task compatibility mapping/tests plus chat-first contract guardrail tests.

## Results
- `GOPROXY=off go test ./...` passed after Stage 02 contract implementation.
- `GOPROXY=off go test ./...` passed after Stage 03 local readiness/security-boundary implementation and supervisor foundation repair.
- `GOPROXY=off go test ./...` passed after Stage 05 `StartChatRun` boundary implementation.
- `GOPROXY=off go test ./...` passed after the Stage 05 idempotency scope repair for same-key/different-`client_message_id` conflict.
- `GOPROXY=off go test -count=1 ./gateway/internal/chatstate ./gateway/internal/chatruntime ./gateway/internal/domain ./gateway/internal/grpcapi` and `GOPROXY=off go test -count=1 ./...` passed after Stage 06 existing-chat behavior and owner-review repairs.
- `GOPROXY=off go test -count=1 .\gateway\internal\appserver .\gateway\internal\chatstate .\gateway\internal\chatruntime .\gateway\internal\grpcapi`, `GOPROXY=off go test -count=1 ./...`, and `git diff --check` passed after Stage 07 pending/interrupt implementation.
- `GOPROXY=off go test -count=1 .\gateway\internal\chatstate .\gateway\internal\chatruntime`, `GOPROXY=off go test -count=1 .\gateway\internal\appserver .\gateway\internal\chatstate .\gateway\internal\chatruntime .\gateway\internal\grpcapi`, `GOPROXY=off go test -count=1 ./...`, and `git diff --check` passed after Stage 07 owner-review finding repair.
- The same targeted/full checks passed after the second Stage 07 repair for store-level conditional pending completion and expired wrong-shape pending cleanup.
- The same targeted/full checks passed after the third Stage 07 repair for atomic pending registration/transition and passive expired raw pending cleanup.
- `GOPROXY=off go test -count=1 .`, `GOPROXY=off go test -count=1 .\examples\api-handler`, `GOPROXY=off go test -count=1 ./...`, `GOPROXY=off go list ./...`, `GOPROXY=off go vet ./...`, and `git diff --check` passed after Stage 08 Go SDK/adoption implementation and targeted owner-review repairs.
- `GOPROXY=off go test -count=1 .\gateway\internal\chatstate .\gateway\internal\chatruntime`, `GOPROXY=off go test -count=1 . .\examples\api-handler .\gateway\internal\appserver .\gateway\internal\chatstate .\gateway\internal\chatruntime .\gateway\internal\grpcapi`, `powershell -ExecutionPolicy Bypass -File .\scripts\generate-proto.ps1`, `gofmt -w .\gen`, `GOPROXY=off go test -count=1 ./...`, `GOPROXY=off go list ./...`, `GOPROXY=off go vet ./...`, `git diff --check`, and scoped file-tools guardrail grep checks passed for Stage 09 integration hardening local closeout evidence after engineering/QA repair.
- `GOPROXY=off go test ./...` passed after Stage 10 volatile correlation foundation implementation.
- `GOPROXY=off go test -count=1 -run 'Test(GetChatRejectsInvalidUnreadableMismatchedAndUnauthorizedThread|GetChatStatusReadsCodexTurnsForLastRunLifecycle|GetChatStatusReconcilesLocalActiveRunFromCodexTerminalTurn|PostRestartIdempotencyKeyOnlyDoesNotReplaySideEffectingChatResults|ExistingChatOperationsRejectWorkspaceMismatchBeforeRuntimeStateOrCodexSideEffects|CodexBackedExistingChatOperationsRejectThreadOutsideWorkspaceBeforeSideEffects)$' .\gateway\internal\chatruntime`, `GOPROXY=off go test -count=1 .\gateway\internal\chatruntime`, `GOPROXY=off go test -count=1 .\gateway\internal\grpcapi`, `GOPROXY=off go test -race -count=1 .\gateway\internal\chatruntime .\gateway\internal\grpcapi`, `GOPROXY=off go vet ./...`, `GOPROXY=off go test -count=1 ./...`, and `git diff --check` passed after the QA gap-closure repair for `TC-005`, `TC-009`, `TC-027`, and `TC-031`; `git diff --check` reported CRLF warnings only.

## Working-Copy Baseline QA Rerun (2026-06-16)
- Executed commands passed on the working-copy baseline: `GOPROXY=off go list ./...`; `GOPROXY=off go vet ./...`; `GOPROXY=off go test -count=1 ./...`; `git diff --check`.
- Proto generation sync was rechecked without mutating the workspace by copying `proto/`, `gen/`, and `scripts/` to a temp directory, running `powershell -ExecutionPolicy Bypass -File .\\scripts\\generate-proto.ps1`, applying `gofmt -w` only inside that temp copy, and comparing the generated files back to `gen/codex/control/v1/*` with `git diff --no-index --exit-code`; comparison passed.
- Stage 09 guardrail grep reruns passed for working-copy source paths: no `codex-control-gateway` implementation references outside docs, no `github.com/Dirard/codex-runtime/internal/` imports in Go files, no SQLite/local DB markers in Go files, and no Desktop UI/current-thread promise markers in Go files.
- The rerun proves the baseline automation and guardrails still pass, but it does not upgrade the overall QA verdict to full because several required `TC-*` cases still lack direct executable evidence at the required behavior level.
- Cases with executable coverage confirmed in this rerun or still-supported by current automated anchors: `TC-001`, `TC-002`, `TC-003`, `TC-004`, `TC-006`, `TC-007`, `TC-008`, `TC-011`, `TC-012`, `TC-013`, `TC-014`, `TC-016`, `TC-018`, `TC-019`, `TC-020`, `TC-024`, `TC-025`, `TC-026`, `TC-032`, `TC-033`, `TC-034`, `TC-035`, `TC-036`.
- Required cases still not fully proven from executable tests/current evidence on the working-copy baseline: `TC-005`, `TC-009`, `TC-010`, `TC-015`, `TC-017`, `TC-021`, `TC-022`, `TC-023`, `TC-027`, `TC-028`, `TC-029`, `TC-030`, `TC-031`.
- Corresponding regression coverage remains partial for `REG-004`, `REG-005`, `REG-007`, `REG-008`, `REG-009`, and `REG-010`. `REG-001`, `REG-003`, `REG-006`, `REG-011`, and `REG-012` remain supported by the current automated evidence set. `REG-002` is supported for core identity invariants, but restart-recovery proof still depends on the blocked `TC-027` evidence gap.

## QA Repair Follow-up (2026-06-16)
- Focused reruns passed for the repaired coverage slice: `GOPROXY=off go test -count=1 -run 'Test(GetChat(ReadsCodexThreadWithoutResumeOrTurnStart|UnknownCodexThreadReturnsNotFound|UnavailableCodexReadReturnsUnavailable|StatusMapsCodexAndGatewayLifecycle|StatusRejectsInvalidUnknownUnavailableAndMismatchedState)|PostRestartKeepsCodexBackedLookupsButLosesProcessLocalState|StreamChatEvents(ReplaysAfterValidEventCursor|ReturnsReplayUnavailableAfterRestartEpoch|ReturnsCursorEvictedNotice)|InterruptChatRun(SendsCodexInterruptAndKeepsRunNonTerminal|RejectsNoActiveMismatchAndAlreadyInterruptingBeforeCodexCall))$' ./gateway/internal/chatruntime`; `GOPROXY=off go test -count=1 -run 'Test(ChatRuntimeUnaryRequiresAuthBeforeStage03Handler|ChatRuntimeAllRPCsRequireAuthBeforeHandler|AuthenticatedChatRuntimeUnarySanitizesAuthorizationMetadataBeforeService|AuthenticatedChatRuntimeAllRPCsSanitizeAuthorizationMetadataBeforeService|ChatRuntimeHandlersRejectInvalidSessionWorkspaceBeforeRuntime)$' ./gateway/internal/grpcapi`.
- Full baseline rerun also passed: `GOPROXY=off go list ./...`; `GOPROXY=off go vet ./...`; `GOPROXY=off go test -count=1 ./...`; `git diff --check`.
- The earlier isolated flake note did not reproduce in a focused stress rerun: `GOPROXY=off go test -count=10 -run TestPendingServerRequestResolvedClearsActivePending ./gateway/internal/tasks`.
- Newly supported blocker closures from direct executable evidence at this point: `TC-010`, `TC-015`, `TC-017`, `TC-021`, `TC-022`, `TC-023`, `TC-028`, `TC-029`, and `TC-030`.
- This first follow-up still left `TC-005`, `TC-009`, `TC-027`, and `TC-031` pending until the later chat-runtime gap-closure repair below.

## QA Gap Closure Follow-up (2026-06-16)
- Focused inspection and reruns passed for the remaining previous blockers: `GOPROXY=off go test -count=1 -run 'Test(GetChatRejectsInvalidUnreadableMismatchedAndUnauthorizedThread|GetChatStatusReadsCodexTurnsForLastRunLifecycle|GetChatStatusReconcilesLocalActiveRunFromCodexTerminalTurn|PostRestartIdempotencyKeyOnlyDoesNotReplaySideEffectingChatResults|ExistingChatOperationsRejectWorkspaceMismatchBeforeRuntimeStateOrCodexSideEffects|CodexBackedExistingChatOperationsRejectThreadOutsideWorkspaceBeforeSideEffects)$' .\gateway\internal\chatruntime`; `GOPROXY=off go test -count=1 .\gateway\internal\chatruntime`; `GOPROXY=off go test -count=1 .\gateway\internal\grpcapi`; `GOPROXY=off go test -race -count=1 .\gateway\internal\chatruntime .\gateway\internal\grpcapi`; `GOPROXY=off go vet ./...`; `GOPROXY=off go test -count=1 ./...`; `git diff --check`.
- `TC-005` is closed by direct `GetChat` coverage for read-only success plus missing/invalid/unavailable/malformed/id-mismatch/outside-workspace variants, with no chat creation or side-effect calls.
- `TC-009` is closed by `GetChatStatus` coverage for idle/active/system-error/unknown status plus latest Codex turn mapping for completed, failed, interrupted, and in-progress states using `IncludeTurns=true`.
- `TC-027` is closed by restart-loss coverage for Codex-backed chat/status/history recovery, replay-unavailable and pending-unavailable behavior, plus explicit key-only restart checks showing `StartChatRun` and `RunChatTurn` do not replay stale side-effecting results and `InterruptChatRun` rejects stale key-only state before Codex calls.
- `TC-031` is closed by direct existing-chat scope checks: explicit workspace mismatch is rejected before Codex/local side effects for `GetChat`, `RunChatTurn`, `GetChatStatus`, `GetChatHistory`, `StreamChatEvents`, `RespondChatPending`, and `InterruptChatRun`; Codex-proven foreign workspace threads return `PERMISSION_DENIED` for `GetChat`, `GetChatStatus`, `GetChatHistory`, and `RunChatTurn` before turn/list/side-effect calls.
- No remaining previous QA blockers are open for the Stage 09 chat-runtime evidence slice. The broader full proof set remains partial only because this artifact still does not claim an end-to-end execution pass for every planned `TC-001`..`TC-036` and `REG-001`..`REG-012` case.

## QA Product Caveat Follow-up (2026-06-16)
- Focused inspection and reruns passed after the raw-public-contract repair: `GOPROXY=off go test -count=1 -run "Test(ChatRuntimeEventContractHasNoRawPayloadEscapeHatch|PublicProtoContractHasNoRawPayloadEscapeHatch)$" .\gateway\internal\grpcapi`; `GOPROXY=off go test -count=1 -run "Test(StructuredDropsAndUnknownFallbackDoNotExposeRawPayloads|UnknownNotificationWarningSuppressesConnectionScopedPayload)$" .\gateway\internal\tasks`; `GOPROXY=off go test -count=1 .\gateway\internal\grpcapi .\gateway\internal\tasks .\gateway\internal\chatruntime`; `GOPROXY=off go test -race -count=1 .\gateway\internal\chatruntime .\gateway\internal\grpcapi .\gateway\internal\tasks`; `GOPROXY=off go list ./...`; `GOPROXY=off go vet ./...`; `GOPROXY=off go test -count=1 ./...`; `git diff --check`.
- The public task proto no longer exposes `UnknownRawEvent`, `payload_json`, or `unknown_raw`; `TaskEvent` reserves field `21` and uses typed `GatewayWarningEvent gateway_warning = 20` for unsupported task notifications.
- Executable guardrails now include whole-public-proto descriptor scanning for raw payload escape-hatch names, plus task-service tests proving unknown notifications emit typed `GatewayWarningEvent` with `payload_suppressed` and do not serialize raw sensitive payload fragments.
- File-tools grep found the raw legacy identifiers only in historical review notes and guardrail test strings. No previous Stage 09 clean evidence regressed in the rerun.

## QA Decision
targeted Stage 09 chat-runtime QA evidence is clean for the previous blockers; full `TC-001`..`TC-036` / `REG-001`..`REG-012` QA coverage is still not claimed by this artifact

## Rerun Trigger
Continue execution if implementation behavior, product-doc current behavior, or owning decisions change. Full QA should run against the current-behavior docs baseline for the working-copy implementation snapshot.

## Forbidden Content
- Claims that all planned tests were executed or that partial Stage 02 checks mean Done.
- Raw secrets, auth headers, token values, cookies, private keys, passwords, raw env values, raw JSONL, or private data.
